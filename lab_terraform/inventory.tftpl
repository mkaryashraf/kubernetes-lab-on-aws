[cp]
cp ansible_host=${cp} ansible_user=ec2-user ansible_ssh_private_key_file=~/.ssh/${key_name}.pem

[workers]
%{ for index, ip in workers ~}
w-${index + 1} ansible_host=${ip} ansible_user=ec2-user ansible_ssh_private_key_file=~/.ssh/${key_name}.pem
%{ endfor ~}

[kube_nodes:children]
cp
workers
